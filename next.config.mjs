/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "fmmkfdfyvkpsellnladk.supabase.co",
      },
    ],
  },
};

export default nextConfig;
